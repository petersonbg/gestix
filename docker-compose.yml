services:
  db:
    image: postgres:16-alpine
<<<<<<< HEAD
=======
    container_name: gestix_db
>>>>>>> 027f04bc6b4f2b33d16a13e0d7c9548c220798f7
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-gestix}
      POSTGRES_USER: ${DB_USER:-gestix}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-gestix123}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-gestix} -d ${DB_NAME:-gestix}"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build: .
<<<<<<< HEAD
=======
    container_name: gestix_web
>>>>>>> 027f04bc6b4f2b33d16a13e0d7c9548c220798f7
    restart: unless-stopped
    command: sh -c "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py runserver 0.0.0.0:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-alterar-esta-chave}
      DEBUG: ${DEBUG:-False}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-localhost,127.0.0.1,192.168.1.50}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS:-http://localhost:8000,http://127.0.0.1:8000,http://192.168.1.50:8000}
      USE_HTTPS: ${USE_HTTPS:-False}
      SERVE_MEDIA_FILES: ${SERVE_MEDIA_FILES:-True}
      DB_NAME: ${DB_NAME:-gestix}
      DB_USER: ${DB_USER:-gestix}
      DB_PASSWORD: ${DB_PASSWORD:-gestix123}
      DB_HOST: db
      DB_PORT: 5432
<<<<<<< HEAD
      RUNNING_IN_DOCKER: "True"
      BACKUP_ROOT: /app/backups
    volumes:
      # O collectstatic recria /app/staticfiles; não monte um volume vazio nesse caminho.
      - .:/app
      - ./backups:/app/backups
    ports:
      - "${WEB_PORT:-8000}:8000"
=======
    volumes:
      # O collectstatic recria /app/staticfiles; não monte um volume vazio nesse caminho.
      - .:/app
    ports:
      - "8000:8000"
>>>>>>> 027f04bc6b4f2b33d16a13e0d7c9548c220798f7
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
